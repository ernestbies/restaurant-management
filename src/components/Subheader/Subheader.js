import {StyledSubheader} from "./Subheader.styles";
import React from "react";

const Subheader = () => {
    return (<StyledSubheader>{'Restaurant Management System (RMS) allows you to add and modify products as well as manage the database from the level of a web browser.'}</StyledSubheader>)
};

export default Subheader;
